// Fix: Comment out missing vite/client types to resolve compiler error.
// The application does not currently rely on Vite-specific types like import.meta.env.
// /// <reference types="vite/client" />

export {};